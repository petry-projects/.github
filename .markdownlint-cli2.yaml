config:
  MD013: false   # Line length — many existing docs exceed 200 chars; fix incrementally
  MD024: false   # Duplicate headings — AGENTS.md has intentional repeated section headers
  MD032: false   # Blanks around lists — many existing docs use compact list formatting
  MD033: false   # Inline HTML — used in standards docs for tables/details
  MD034: false   # Bare URLs — AGENTS.md uses bare URLs for references
  MD041: true    # First line must be a top-level heading
