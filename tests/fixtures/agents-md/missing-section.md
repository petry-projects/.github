# Missing Section Fixture

Structurally valid EXCEPT the recommended Security section is absent. Because
section-presence rules are `recommended`, this must surface a recommended
(informational) finding but never a required one — it must not fail `--mode
failing`. Cross-reference resolves: [CLAUDE.md](./CLAUDE.md).

## Development Standards

Standards section present.

## Testing

Testing section present. No Security section anywhere in this file.
