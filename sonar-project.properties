sonar.projectKey=petry-projects_.github
sonar.organization=petry-projects
# Exclude daily-org-status.yml from SonarCloud analysis: npm install postinstall
# scripts are required to download the claude binary and cannot use --ignore-scripts.
sonar.exclusions=.github/workflows/daily-org-status.yml
