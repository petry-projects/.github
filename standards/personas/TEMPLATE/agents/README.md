# Copilot profiles for this persona

Drop a single-file `<id>.md` Copilot profile here (frontmatter: `name`, `description`,
`tools`) if `definition.layers[]` includes a `copilot-profile` layer. Otherwise delete
this directory. See `standards/agent-standards.md`.
