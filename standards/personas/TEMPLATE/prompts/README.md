# Workflow prompt library for this persona

Phase-keyed prompts (e.g. `on-mention.md`, `review-changes.md`) filled by a shell
orchestrator, if `definition.layers[]` includes a `workflow-prompts` layer. Otherwise
delete this directory. Model the layout on `.github-private/prompts/dev-lead/`.
