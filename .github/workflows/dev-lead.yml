# ─────────────────────────────────────────────────────────────────────────────
# Dev-Lead Agent — thin caller stub
# Standard:  petry-projects/.github/standards/ci-standards.md#5-dev-lead-agent
# Reusable:  petry-projects/.github-private/.github/workflows/dev-lead-reusable.yml
#
# ADOPTING THIS WORKFLOW:
#   1. Copy this file verbatim to .github/workflows/dev-lead.yml in your repo.
#   2. Ensure CLAUDE_CODE_OAUTH_TOKEN is set as an org or repo secret.
#   3. Optionally set GH_PAT_WORKFLOWS (required if Claude pushes workflow files).
#   4. Optionally set vars.DEV_LEAD_ENGINE = "claude" | "gemini" | "copilot".
#
# UNLIKE claude.yml, this file has NO OIDC byte-for-byte constraint and may be
# freely modified on PR branches to adjust triggers for repo-specific needs.
#
# REQUIRED secrets: CLAUDE_CODE_OAUTH_TOKEN
# OPTIONAL secrets: GH_PAT_WORKFLOWS, GOOGLE_API_KEY, GH_PAT
# ─────────────────────────────────────────────────────────────────────────────

name: Dev-Lead Agent

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created]
  issue_comment:
    types: [created]
  issues:
    types: [labeled]
  check_run:
    types: [completed]
  repository_dispatch:
    types: [dev-lead-ci-failure]

permissions: {}

<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
# Concurrency is centralised in the reusable workflow (dev-lead-reusable.yml) with
# per-issue / per-PR lanes, so issue pickups are never cancelled by PR follow-up
# traffic and the grouping can't drift per-repo. See petry-projects/.github#402.

=======
>>>>>>> e6d47e0 (feat(dev-lead): adopt dev-lead agent (Phase 8 cross-repo rollout))
=======
concurrency:
  # One active run per repo; ci-relay (check_run) keeps an ephemeral per-SHA slot
  # so it can fire immediately without blocking or being blocked by the dispatch queue.
  group: >-
    ${{
      github.event_name == 'check_run' && format('dev-lead-ci-relay-{0}', github.event.check_run.head_sha) ||
      'dev-lead'
    }}
  cancel-in-progress: false
=======
# Concurrency is centralised in the reusable workflow (dev-lead-reusable.yml) with
# per-issue / per-PR lanes, so issue pickups are never cancelled by PR follow-up
# traffic and the grouping can't drift per-repo. See petry-projects/.github#402.
>>>>>>> ae0a8e4 (fix(dev-lead): add statuses:read (fixes startup_failure) + centralise concurrency [template + .github stub] (#403))

>>>>>>> 6306793 (feat(concurrency): add per-repo serialized concurrency to dev-lead stubs (#322))
jobs:
  dev-lead:
    uses: petry-projects/.github-private/.github/workflows/dev-lead-reusable.yml@main
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
      issues: write
      actions: read
      checks: read
<<<<<<< HEAD
<<<<<<< HEAD
      statuses: read   # required by dev-lead-reusable.yml since #435
=======
>>>>>>> e6d47e0 (feat(dev-lead): adopt dev-lead agent (Phase 8 cross-repo rollout))
=======
      statuses: read   # required by dev-lead-reusable.yml since #435
>>>>>>> ae0a8e4 (fix(dev-lead): add statuses:read (fixes startup_failure) + centralise concurrency [template + .github stub] (#403))
